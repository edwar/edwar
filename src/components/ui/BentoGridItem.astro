---
import { cn } from "@/lib/utils";
import { Icon } from "astro-icon/components";
import { Image } from "astro:assets";
const { class: className, image, icons, title, description, link } = Astro.props;
---
<div
  class={cn(
    "group/bento shadow-input flex flex-col rounded-xl border border-slate-200 dark:border-white/20 bg-white/70 dark:bg-slate-800/60 backdrop-blur-sm shadow-none transition duration-500 hover:shadow-xl overflow-hidden h-full",
    className,
  )}
>
  {image && (
    <div class="min-h-60 max-h-60 overflow-hidden">
      <Image 
        src={image} 
        alt={title} 
        width={900} 
        height={600} 
        class="w-full h-full object-cover transition-transform duration-300 scale-105 group-hover/bento:scale-110" 
      />
    </div>
  )}
  
  <div class="flex flex-col flex-1 p-6 transition duration-200 group-hover/bento:translate-x-2">
    <div class="flex flex-wrap gap-2 mb-4">
      {Array.isArray(icons) && icons.map((icon: string, index: number) => (
        <div class="flex items-center justify-center p-1.5 rounded-lg bg-white/90 dark:bg-white/90 backdrop-blur-sm hover:bg-white transition-all duration-300 hover:scale-110 hover:shadow-lg hover:shadow-fuchsia-600 cursor-pointer border border-slate-200 dark:border-transparent">
          <Icon name={icon} width="24" height="24" class="text-white" />
        </div>
      ))}
    </div>
    
    <h3 class="mb-2 text-xl font-bold text-slate-900 dark:text-white transition-colors duration-500">
      {title}
    </h3>
    
    <p class="text-sm text-slate-600 dark:text-gray-300 flex-1 transition-colors duration-500">
      {description}
    </p>
    
    <div class="mt-4 pt-4 border-t border-slate-200 dark:border-white/10 transition-colors duration-500">
      <a href={link} target="_blank" rel="noopener noreferrer" class="text-sm font-medium text-fuchsia-600 dark:text-fuchsia-400 hover:text-fuchsia-800 dark:hover:text-fuchsia-300 transition-colors cursor-pointer">
        View Project →
      </a>
    </div>
  </div>
</div>